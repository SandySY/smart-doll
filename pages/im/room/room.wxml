<view class="container" style="{{containerStyle}}">
    <chatroom collection="{{chatRoomCollection}}" envId="{{chatRoomEnvId}}" getOpenID="{{getOpenID}}" groupId="{{chatRoomGroupId}}" groupName="{{chatRoomGroupName}}" onGetUserInfo="{{onGetUserInfo}}" style="width: 100%; height: 100%" userInfo="{{userInfo}}"></chatroom>
</view>
